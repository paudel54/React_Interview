import React from 'react'

const ListItem = (props) => {
    return (
        <div>
            <ul>
                <li>{props.pName}</li>
            </ul>
        </div>
    )
}

export default ListItem
